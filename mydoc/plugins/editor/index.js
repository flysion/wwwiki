import __ from './../../../lib/create_element';
import './index.css';

window.editor = function($options) {
    $options = $.extend({

    }, $options);

    return (o, $event, $el) => {

    };
};